#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a;

        for (a = 'a'; a <= 'z'; a++){
        putchar(a);
	for (A = 'A'; A <= 'Z'; A++)
	putchat(A);
	}
        putchar('\n');
	return (0);
} 

