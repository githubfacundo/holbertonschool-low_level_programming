#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char z;
for (z = 'z'; a <= 'a'; z--)
{
putchar(z);
}
putchar('\n');
return (0);
}i
